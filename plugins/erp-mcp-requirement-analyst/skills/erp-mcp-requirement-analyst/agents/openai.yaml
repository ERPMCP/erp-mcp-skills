interface:
  display_name: "ERP MCP 需求助手 V2"
  short_description: "澄清ERP口径，生成交互HTML看板"
  brand_color: "#007AFF"
  default_prompt: "Use $erp-mcp-requirement-analyst to clarify this ERP MCP reporting request, avoid fake data, use only allowed exports, and generate an interactive HTML result."

dependencies:
  tools:
    - type: "mcp"
      value: "erp"
      description: "Fangzaixian ERP MCP read-only tools for statistics, contract finance details, house listings, and section market data."

policy:
  allow_implicit_invocation: true
