interface:
  display_name: "ERP MCP 快速数据助手 V4"
  short_description: "先问关键定义，再出数字，点击后才读明细"
  brand_color: "#007AFF"
  default_prompt: "Use $erp-mcp-requirement-analyst. For new-listing requests, ask only the missing result-changing question without reading files or probing ERP, then call only the bundled dashboard proxy. Show concrete Chinese examples instead of abstract hierarchy terms."

dependencies:
  tools:
    - type: "mcp"
      value: "erp_dashboard_proxy"
      description: "Bundled MCP Apps proxy for quick summaries, plain-language filters, click-only calculations, and click-only paginated details."

policy:
  allow_implicit_invocation: true
